1. Exercism - Day 1 - Hello World! 

String getGreeting() {
    return "Hello, World!";
}





2. Write a program to demonstrate compatible type conversions. For eg., float to int, double to float, int to short.
  
package touch;

public class oldwood {
public static void main (String []args) {
	byte a= 10;
	short b=a;
	int c=4;
	int f;      //float to int
	long d=139876544l;
	float e= d;
	f=(int)e;   // float to end
	float s =(float)d;
	char g=16;
	long n=(long)d;   // double to long
	
}
}

____________________________________________________________________________________________________________________________________________________________________________

4.Write a Java program that gets a number from the user and displays the name of the weekday. Use enum.
  
import java.util.Scanner;

public class school {
    
    enum Weekday {
        SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number (1-7) to get the weekday: ");
        int a = scanner.nextInt();

        if (a < 1 || a> 7) {
            System.out.println("Invalid input");
        } else {
            Weekday b= Weekday.values()[a - 1];
            System.out.println("The weekday is: " + weekday);
        }

        scanner.close();
    }
}


______________________________________________________________________________________________________________________________________________________________________________________________________________

5. Write a program that calculates the average weight of 10 people. Use descriptive and meaningful variable names following Java naming conventions. Use proper datatypes for the variables.
  

